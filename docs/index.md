# TokenRouter 工程文档

> 本文件是 Project Doc 的根目录。当前会话加载 `project-doc` 技能后，每个仓库任务都先完整读取本文件，再按“读取时机”进入相关分类。

## 文档范围

本库记录 TokenRouter 需要跨越后端、前端、数据存储和部署边界才能理解的持久工程知识。面向最终用户的操作指南、法律文本、生成代码、临时计划和可由单个符号直接读出的局部实现不属于本库；位于 `docs/` 但未被本目录或分类目录规范列出的文件也不自动成为 Project Doc 成员。

## 根级文档

- [项目总览](project_overview.md)：项目定位、仓库边界、运行形态和核心术语。读取时机：首次接触仓库、判断变更归属或需要全局上下文时读取。

## 分类

- [架构](architecture/index.md)：系统组成、进程启动、依赖层次和网关请求链路。读取时机：修改应用装配、服务边界、请求转发或故障转移时读取。
- [领域](domains/index.md)：身份与租户、路由与计费、支付权益和异步任务规则。读取时机：修改核心实体、业务不变量、状态转换或结算规则时读取。
- [接口](interfaces/index.md)：HTTP 路由、配置来源和上游平台适配契约。读取时机：修改外部 API、配置项、协议转换或第三方集成时读取。
- [运维](operations/index.md)：部署迁移、观测维护、开发流程和入口安全。读取时机：构建、发布、迁移、排障、数据维护或同步上游时读取。
